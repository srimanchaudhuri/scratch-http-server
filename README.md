# scratch-http-server
An educational HTTP server written from scratch in C, progressively implementing core web server features including TCP sockets, HTTP request parsing, routing, basic concurrency, and static file serving.
